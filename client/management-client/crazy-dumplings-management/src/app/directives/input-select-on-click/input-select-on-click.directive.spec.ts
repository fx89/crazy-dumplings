import { InputSelectOnClickDirective } from './input-select-on-click.directive';

describe('InputSelectOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSelectOnClickDirective(null);
    expect(directive).toBeTruthy();
  });
});
